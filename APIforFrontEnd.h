// что будет

/*для компиляции 
int GetMapLength();

int GetMapHeight();

ResourseType GetResourseOnGivenCell(int x, int y);

TerrainType GetTerrainOnGivenCell(int x, int y);

BuildingType GetBuildingOnGivenCell(int x, int y);

UnitType GetUnitOnGivenCell(int x, int y);

// что нам (ВАМ) нужно
void SetBuilding(BuildingType, int x, int y); // рисует на карте в нужном месте нужное здание

void SetUnit(UnitID, int x, int y); // поставить юнита

void MoveUnit(UnitID, int new_x, int new_y); // передвинуть юнита
*/
// void StartActivityAnimation;
// void EndActivityAnimation;

// по клику на иконку "начать активность" отослать соответсвующий сигнал